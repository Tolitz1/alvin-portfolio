{
    "version": 2,
    "builds": [
        {
            "src": "your-source-directory",
            "use": "@vercel/static-build",
            "config": {
                "buildCommand": "npm run build"
            }
        }
    ]
}